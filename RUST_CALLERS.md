# Rust callers — `sentry-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `sentry` | `sentry` | 11 | 1 | 1 | 11 |

## `sentry`

### Inbound symbols (11 distinct, 11 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `sentry`.

#### `sentry::Client` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:391` — `use sentry::Client`

#### `sentry::ClientOptions` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:393` — `use sentry::ClientOptions`

#### `sentry::protocol::Attachment` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:513` — `use sentry::protocol::Attachment`

#### `sentry::protocol::Envelope` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:394` — `use sentry::protocol::Envelope`

#### `sentry::protocol::EnvelopeItem` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:395` — `use sentry::protocol::EnvelopeItem`

#### `sentry::protocol::Event` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:396` — `use sentry::protocol::Event`

#### `sentry::protocol::Exception` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:434` — `use sentry::protocol::Exception`

#### `sentry::protocol::Level` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:397` — `use sentry::protocol::Level`

#### `sentry::protocol::Values` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:435` — `use sentry::protocol::Values`

#### `sentry::transports::DefaultTransportFactory` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:398` — `use sentry::transports::DefaultTransportFactory`

#### `sentry::types::Dsn` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-feedback` `src/lib.rs:399` — `use sentry::types::Dsn`
